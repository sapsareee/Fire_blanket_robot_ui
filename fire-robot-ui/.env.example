# Optional overrides for network access from other PCs
# If omitted, the dashboard uses the host that served the page.

# ROS bridge endpoint
VITE_ROS_HOST=192.168.0.10
VITE_ROSBRIDGE_PORT=9090

# web_video_server endpoint
VITE_VIDEO_HOST=192.168.0.10
VITE_VIDEO_PORT=8080
